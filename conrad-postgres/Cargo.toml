[package]
name = "conrad-postgres"
version = "0.1.0"
edition = "2021"

[dependencies]
diesel = { version = "2.1", features = ["postgres"] }
diesel_migrations = { version = "2.1", features = ["postgres"] }
dotenvy = "0.15"
thiserror = "1.0.40"
